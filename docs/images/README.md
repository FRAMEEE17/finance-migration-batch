# Demo images

Drop real screenshots here, matching the filenames the root `README.md`
already references (currently commented out until the files exist):

- `airflow-dag-graph.png` - Airflow's graph view for `gl_period_close`,
  mid-run or after a real trigger
- `duckdb-ui.png` - `duckdb -ui warehouse.duckdb`, tables panel open
- `period-report-sample.png` - a signed period report or controller pack

Once a file exists, uncomment the matching `<!-- ![...] -->` line in the
root README.
