duckdb==1.4.5

# Optional, only needed to re-execute notebooks/*.ipynb, not to run a
# period through the pipeline. Installing these pulls jupyter_client and
# jupyter_core in as dependencies.
# nbconvert==7.17.1
# ipykernel==6.31.0
